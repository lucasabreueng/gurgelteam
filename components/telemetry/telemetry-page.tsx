"use client";

import { TelemetryComparison } from "@/components/student-area/telemetry-comparison";

/** Conteúdo principal — comparação de voltas (piloto e admin). */
export function TelemetryPage() {
  return <TelemetryComparison />;
}
